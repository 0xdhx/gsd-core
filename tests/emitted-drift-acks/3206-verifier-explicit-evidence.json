{
  "version": 1,
  "paths": {
    "agents/gsd-verifier.md": {
      "reason": "#3206: Step 3 item 5b used 'explicit evidence' undefined — the definition lived only in the non-included gsd-core/references/honest-verifier.md, behind a stale bare `references/` cite that 404s. 5b now carries the compressed definition inline (a passing wired held-out/property-based test or directly observed behavior; presence + wiring never qualifies) and all three bare honest-verifier/verify-mvp-mode cites in the file gained the repo-conventional gsd-core/references/ prefix. Growth is the literal fix: 49049 -> 49151 bytes, 1 byte under the LARGE cap (49152) — the cap-hugging is pre-existing (#1955 consumed the #3058 reclaim) and is why the AFK-reporting and insufficient_spec-distinctness clauses land in the eagerly-included verifier-phase-gates.md instead of inline."
    },
    "gsd-core/references/verifier-phase-gates.md": {
      "reason": "#3206: gains the backstop-abstention reporting contract (AFK never-silent/never-halt completion line, insufficient_spec kept distinct from manual-UAT human_needed) — the two honest-verifier.md clauses that must be in the verifier's guaranteed context (AC3/AC4) but cannot fit agents/gsd-verifier.md's 1-byte cap headroom. This file is already in <required_reading>, so the clauses become guaranteed-context without touching the capped agent file. Also extends the infrastructure-phase gate's exception set so an abstained insufficient_spec truth rides the same carve-out as PRESENT_BEHAVIOR_UNVERIFIED, and binds the infra N/A log template to the no-excepted-truth case — resolving the contradiction the pre-create adversarial review found between the new contract and the infra auto-pass shortcut."
    },
    "agents/gsd-verifier.agent.md": {
      "reason": "#3206: derived per-runtime emission of agents/gsd-verifier.md — ripple of the 5b explicit-evidence definition and cite repairs above, no independent change."
    },
    "agents/subagents/gsd-verifier.md": {
      "reason": "#3206: kimi-runtime derived emission of agents/gsd-verifier.md — ripple of the 5b explicit-evidence definition and cite repairs, no independent change."
    }
  }
}
